import React, { FC } from 'react';

import { Container } from './separator.styles';

/* -------------------------------------------------------------------------- */

const Separator: FC = () => <Container />;

export default Separator;
