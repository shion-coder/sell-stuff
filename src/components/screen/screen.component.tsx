import React, { FC } from 'react';

import { Container } from './screen.styles';

/* -------------------------------------------------------------------------- */

const Screen: FC = ({ children }) => <Container>{children}</Container>;

export default Screen;
